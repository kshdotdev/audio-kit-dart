name: Publish audio_kit_graph

on:
  push:
    tags:
      - 'audio_kit_graph-v[0-9]+.[0-9]+.[0-9]+'
      - 'audio_kit_graph-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: audio_kit_graph
      working-directory: packages/audio_kit_graph
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/audio_kit_graph
      environment: pub.dev
