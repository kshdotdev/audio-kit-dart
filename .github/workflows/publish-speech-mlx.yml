name: Publish speech_mlx

on:
  push:
    tags:
      - 'speech_mlx-v[0-9]+.[0-9]+.[0-9]+'
      - 'speech_mlx-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: speech_mlx
      working-directory: packages/speech_mlx
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/speech_mlx
      environment: pub.dev
