name: Publish audio_aec

on:
  push:
    tags:
      - 'audio_aec-v[0-9]+.[0-9]+.[0-9]+'
      - 'audio_aec-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: audio_aec
      working-directory: packages/audio_aec
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/audio_aec
      environment: pub.dev
