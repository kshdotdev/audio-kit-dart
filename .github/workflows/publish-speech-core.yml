name: Publish speech_core

on:
  push:
    tags:
      - 'speech_core-v[0-9]+.[0-9]+.[0-9]+'
      - 'speech_core-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: speech_core
      working-directory: packages/speech_core
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/speech_core
      environment: pub.dev
