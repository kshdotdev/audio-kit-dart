name: Publish speech_deepgram

on:
  push:
    tags:
      - 'speech_deepgram-v[0-9]+.[0-9]+.[0-9]+'
      - 'speech_deepgram-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: speech_deepgram
      working-directory: packages/speech_deepgram
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/speech_deepgram
      environment: pub.dev
