name: Publish speech_sherpa

on:
  push:
    tags:
      - 'speech_sherpa-v[0-9]+.[0-9]+.[0-9]+'
      - 'speech_sherpa-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: speech_sherpa
      working-directory: packages/speech_sherpa
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/speech_sherpa
      environment: pub.dev
