name: Publish audio_processing

on:
  push:
    tags:
      - 'audio_processing-v[0-9]+.[0-9]+.[0-9]+'
      - 'audio_processing-v[0-9]+.[0-9]+.[0-9]+-*'

permissions: {}

jobs:
  preflight:
    permissions:
      contents: read
    uses: ./.github/workflows/release-check.yml
    with:
      package: audio_processing
      working-directory: packages/audio_processing
  publish:
    needs: preflight
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      working-directory: packages/audio_processing
      environment: pub.dev
